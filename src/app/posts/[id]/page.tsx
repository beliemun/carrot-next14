import { Button } from "@/components/common";
import Link from "next/link";

export default function Life({ param }: { param: { id: number } }) {
  return <main className="col-center min-h-screen"></main>;
}
